# dumRice

Mi configuración completa de Hyprland.

## Instalación rápida

Para instalar este rice en tu máquina, **solo copia y pega** todo esto en tu terminal:

```bash
git clone https://github.com/dumwaza/dumRice.git
cd dumRice
chmod +x install_dumRice.sh
./install_dumRice.sh
